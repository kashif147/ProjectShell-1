# Copy to `.env` in this directory (see GATEWAY_ENV_CONFIG.md).

# Via gateway (staging/production)
REACT_APP_REPORTING_SERVICE_URL=https://your-gateway-host/reporting-service/api

# Local reporting-service only (no gateway; routes at service root)
# REACT_APP_REPORTING_SERVICE_URL=http://localhost:4005
